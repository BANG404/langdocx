
构建一个工具专门用于 unzip 模板文件，
删除 lsdException 和 latentStyles
过滤掉所有 lsdException 和 latentStyles
正如之前解释的，这两部分内容只定义了 Word 界面中样式的显示逻辑（隐藏还是显示、优先级等），完全不包含任何具体的排版属性（如字号、颜色、字体）。
处理方法： 在将 XML 传给 Agent 之前，使用脚本（Python 或正则）彻底删掉整个 <w:latentStyles> 标签及其内部所有内容。
修改并修改 document.xml
忽略修订标记： 删掉 <w:proofErr>（拼写检查）、<w:lastRenderedPageBreak>（分页符预览）。
移除 Rsid 属性： 删掉所有 w:rsidR, w:rsidRPr 等属性，这些是 Word 内部记录修订 ID 的，对语义理解毫无用处。
合并相邻 Run： Word 经常会因为你中途改了个字就把一句话拆成好几个 <w:r> 标签。预处理时应将同一段落内样式相同的 Run 合并。
